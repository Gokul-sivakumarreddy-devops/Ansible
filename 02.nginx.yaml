- name: nginx and running Nginx
  hosts: web
  become: yes  # will get root access
  tasks:
  - name: Installing nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: nginx
    ansible.builtin.service:
      name: nginx
      state: started
    